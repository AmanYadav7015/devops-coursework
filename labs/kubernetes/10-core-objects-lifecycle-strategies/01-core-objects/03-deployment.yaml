apiVersion: apps/v1
kind: Deployment
metadata:
  name: core-deploy
  namespace: hw10
  labels:
    app: core-deploy
spec:
  replicas: 2
  selector:
    matchLabels:
      app: core-deploy
  template:
    metadata:
      labels:
        app: core-deploy
    spec:
      containers:
        - name: web
          image: nginx:alpine
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: "20m"
              memory: "32Mi"
            limits:
              cpu: "100m"
              memory: "64Mi"
