apiVersion: v1
kind: Service
metadata:
  name: gate-backend
  namespace: hw10
  labels:
    app: gate-backend
spec:
  type: ClusterIP
  selector:
    app: gate-backend
  ports:
    - name: http
      port: 80
      targetPort: 80
