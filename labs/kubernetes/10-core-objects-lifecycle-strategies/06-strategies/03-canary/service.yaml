apiVersion: v1
kind: Service
metadata:
  name: shop
  namespace: hw10
  labels:
    app: shop
spec:
  type: NodePort
  selector:
    app: shop
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30102
      protocol: TCP
