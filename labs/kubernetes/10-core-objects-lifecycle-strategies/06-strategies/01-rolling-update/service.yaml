apiVersion: v1
kind: Service
metadata:
  name: app-rolling
  namespace: hw10
  labels:
    app: app-rolling
spec:
  type: NodePort
  selector:
    app: app-rolling
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30100
      protocol: TCP
