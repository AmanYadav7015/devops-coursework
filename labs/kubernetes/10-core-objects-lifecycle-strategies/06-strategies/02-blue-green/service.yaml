apiVersion: v1
kind: Service
metadata:
  name: myapp
  namespace: hw10
  labels:
    app: myapp
spec:
  type: NodePort
  selector:
    app: myapp
    slot: blue
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30101
      protocol: TCP
