apiVersion: v1
kind: Service
metadata:
  name: broken-backend
  namespace: hw11
spec:
  type: ClusterIP
  selector:
    app: wrong-backend-name
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
